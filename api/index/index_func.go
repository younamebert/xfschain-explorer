package api

import "xfschainbrowser/model"

func meanBlockTxNumber(blockheaders []*model.ChainBlockHeader) {
	// blockAmount := len(blockheaders)
	// blockAmountTx := 0
	// for _, v := range blockheaders {

	// }
}
